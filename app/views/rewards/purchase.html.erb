<h1>Purchase reward: <%=@reward.name%></h1>
<%= link_to("Back to all Rewards", members_path_url, class: 'back-link') %>

<div class="rewards purchase">

  <% # url: task_path(@task), method: 'delete' %>

  <p>Are you sure you want to purchase this reward?</p>
  <p><%= @reward.name %> for <%= @reward.point_value %> point(s).</p>

  <%= link_to("Confirm", handle_purchase_path(@reward.id))%>

</div>

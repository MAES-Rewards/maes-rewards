<h1>Not in use</h1>
<%= link_to("Go back to landing page", member_dashboard_url, aria: {label: "Back to Landing Page Button"})%>
  <thead>
    <tr>
      <th aria-label="Name">Name</th>
      <th aria-label="Points">Points</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><% @user.name %></td>
      <td><% @user.points %></td>
    </tr>
  </tbody>
</table>

<h1>This page is not in use, since we are specifying 1 user</h1>
